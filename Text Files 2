//importing everything I need
import java.util.*;

public class FurtherProcess {
//Contact class
    static class Contact {
        private String name;
        private String email;
        private int gradYear;

        public Contact(String name, String email, int gradYear) {
            this.name = name;
            this.email = email;
            this.gradYear = gradYear;
        }
//getter methods
        public String getName() {
            return name;
        }

        public String getEmail() {
            return email;
        }

        public int getGradYear() {
            return gradYear;
        }
//toString method
        public String toString() {
            return name + ", " + email + ", " + gradYear;
        }
    }
//main
    public static void main(String[] args) {
        ArrayList<Contact> contacts = new ArrayList<>();

        contacts.add(new Contact("Jeff Papa", "Papa@gmail.com", 2026));
        contacts.add(new Contact("Kate Welch", "welch@yahoo.com", 2025));
        contacts.add(new Contact("Dababy", "dababy@outlook.com", 2027));
//scanner
        Scanner input = new Scanner(System.in);
        boolean running = true;

        while (running) {
            System.out.println("MENU");
            System.out.println("a = add contact");
            System.out.println("e = sort by email");
            System.out.println("y = sort by grad year");
            System.out.println("n = sort by name");
            System.out.println("q = quit");

            String choice = input.nextLine();
//if loop
            if (choice.equals("a")) {
                System.out.print("Name: ");
                String name = input.nextLine();

                System.out.print("Email: ");
                String email = input.nextLine();

                System.out.print("Grad Year: ");
                int year = Integer.parseInt(input.nextLine());
//else if loops
                contacts.add(new Contact(name, email, year));
            } else if (choice.equals("e")) {
                contacts.sort(Comparator.comparing(Contact::getEmail));
            } else if (choice.equals("y")) {
                contacts.sort(Comparator.comparingInt(Contact::getGradYear));
            } else if (choice.equals("n")) {
                contacts.sort(Comparator.comparing(Contact::getName));
            } else if (choice.equals("q")) {
                running = false;
            }
//system print
            System.out.println("CONTACTS");
            for (Contact c : contacts) {
                System.out.println(c);
            }
        }

        System.out.println("Program ended.");
    }
}

